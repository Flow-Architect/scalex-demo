# ScaleX Architecture

Planned local architecture:

- FastAPI backend
- SQLite audit ledger at `data/scalex.db`
- Vite React TypeScript frontend
- Local policy engine
- Mock-first Stripe service
- Deterministic agent outputs with optional AI planning fallback later
- Hermes-style local orchestration adapter later
